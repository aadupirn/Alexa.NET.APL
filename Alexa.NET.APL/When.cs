using System;
using System.Collections.Generic;
using System.Text;

namespace VoicifyAlexa.NET.APL
{
    public static class When
    {
        public const string RoundViewport = "${viewport.shape == 'round'}";
        public const string RectangleViewport = "${viewport.shape == 'rectangle'}";
    }
}
